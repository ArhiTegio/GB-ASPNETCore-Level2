@model WebStore.Domain.ViewModels.ProductViewModel
@{
    ViewData["Title"] = $"Товар {Model.Name}";
}

<div class="container">
    <vc:bread-crumbs />
</div>

<section>
    <div class="container">
        <div class="row">
            <div class="col-sm-3">
                <partial name="Partial/_LeftSideBar" />
            </div>

            <div class="col-sm-9 padding-right">
                <partial name="Partial/_ProductDetails" model="@Model" />

                @*<partial name="Partial/_CategoryTab" model="@Model" />

                <partial name="Partial/_RecomendedItems" model="@Model" />*@

            </div>
        </div>
    </div>
</section>
